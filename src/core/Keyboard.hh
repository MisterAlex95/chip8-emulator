#ifndef KEYBOARD_HH
#define KEYBOARD_HH

#include <cstdint>

namespace chip8
{
    class Keyboard
    {
       public:
        Keyboard();

       private:
    };

}  // namespace chip8

#endif  // KEYBOARD_HH
